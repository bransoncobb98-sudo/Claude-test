# Database
DATABASE_URL="postgresql://txarts:txarts_dev_password@localhost:5432/txarts_pathway"

# Auth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-string"

# Stripe (test mode) — leave blank in dev; checkout will report
# "payments are not configured" instead of crashing.
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Transactional email (see lib/email.ts) — leave blank to log to console.
EMAIL_PROVIDER_API_KEY=""
EMAIL_FROM="TX Arts Pathway <no-reply@txartspathway.com>"

# Public site URL, used for absolute links in emails/Stripe redirect URLs
NEXT_PUBLIC_APP_URL="http://localhost:3000"
